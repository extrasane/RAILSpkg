% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{RAILS-deprecated}
\alias{RAILS-deprecated}
\alias{fun.rails.threeway}
\alias{fun.sub.rails.threeway}
\alias{fun.nps}
\alias{fun.lkd}
\alias{fun.out}
\title{Deprecated functions}
\usage{
fun.rails.threeway(
  dt_agg_aou,
  dt_agg_pums,
  pop_totals,
  names_univar = c("agegroup", "edu", "homeown", "income", "race_eth", "sex", "region"),
  alpha = 0.05,
  nsiz = sum(dt_agg_pums$weight)
)

fun.sub.rails.threeway(
  dt_agg_aou,
  dt_agg_pums,
  subgroup_var = "region",
  names_univar = setdiff(c("agegroup", "edu", "homeown", "income", "race_eth", "sex",
    "region"), subgroup_var),
  alpha = 0.05
)

fun.nps(cat_temp, dt_aou, dt_s, nsiz = sum(dt_s$weight), theta_init = NULL)

fun.lkd(x, names_univar, LKHD, dt_s, dt_aou, m_s)

fun.out(w, ...)
}
\arguments{
\item{dt_agg_aou}{Aggregated non-probability cell counts (column \code{weight}).}

\item{dt_agg_pums}{Aggregated reference-sample cell counts (column \code{weight}).}

\item{pop_totals}{Named numeric vector of population margins.}

\item{names_univar}{Character vector of main-effect variables.}

\item{alpha}{Significance threshold for forward selection.}

\item{nsiz}{Population total the weights are scaled to.}

\item{subgroup_var}{Name of the stratifying variable.}

\item{cat_temp}{A one-sided model formula.}

\item{dt_aou}{Aggregated non-probability cell counts.}

\item{dt_s}{Aggregated reference-sample cell counts.}

\item{theta_init}{Optional warm-start coefficient vector.}

\item{x}{Candidate term.}

\item{LKHD}{Log-likelihood of the current model.}

\item{m_s}{Model matrix of the current model on the reference cells.}

\item{w}{Numeric vector of weights.}

\item{...}{Ignored.}
}
\value{
For \code{fun.rails.threeway()} and \code{fun.sub.rails.threeway()}, the wide
data frame described above.
}
\description{
The pre-package function names, kept so that existing analysis scripts keep
running. Each forwards to its replacement and warns once per session.
}
\details{
\describe{
\item{\code{fun.rails.threeway()}}{use \code{\link[=rails]{rails()}}, whose defaults (\code{start = 2},
\code{scope = 3}, \code{fallback = FALSE}) reproduce it exactly}
\item{\code{fun.sub.rails.threeway()}}{use \code{\link[=rails_subgroup]{rails_subgroup()}}}
\item{\code{fun.nps()}}{use \code{\link[=nps_weights]{nps_weights()}}}
\item{\code{fun.lkd()}}{use \code{\link[=gvs_step]{gvs_step()}}}
\item{\code{fun.out()}}{use \code{\link[=weight_summary]{weight_summary()}}}
}

The two estimator shims return the old wide data frame -- \code{dt_agg_aou} with
\code{d_rails}, \code{d_cal1}, \code{d_cal2}, \code{d_nps1}, \code{d_nps2}, \code{d_nps1_rake},
\code{d_nps2_rake}, \code{d_unweighted}, \code{selected_terms} and \code{calibrated_terms}
appended -- not a \code{rails_fit}. The weights themselves are identical to what
\code{\link[=rails]{rails()}} produces on the same input under its defaults; only the container
differs.
}
\keyword{internal}
