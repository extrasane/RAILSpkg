% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rails_subgroup.R
\name{rails_subgroup}
\alias{rails_subgroup}
\title{Subgroup RAILS}
\usage{
rails_subgroup(
  np,
  ref,
  vars,
  by,
  weights_ref = NULL,
  weights_np = NULL,
  aggregated = FALSE,
  start = 2,
  scope = 3,
  alpha = 0.05,
  benchmarks = FALSE,
  fallback = FALSE,
  lifo = c("descending", "ascending"),
  keep_data = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{np, ref, vars, weights_ref, weights_np}{As in \code{\link[=rails]{rails()}}.}

\item{by}{Name of the stratifying variable. It must be a column of both
samples, and is automatically excluded from \code{vars}.}

\item{aggregated, start, scope, alpha, benchmarks, fallback, lifo, keep_data, verbose}{As in \code{\link[=rails]{rails()}}, applied within every subgroup.}

\item{...}{Further arguments passed to \code{\link[=rails]{rails()}}.}
}
\value{
An object of class \code{rails_subgroup_fit}: a list with \code{fits} (one
\code{rails_fit} per level, named by level), \code{weights} (a single vector aligned
to the rows of \code{np}, pooling every subgroup), \code{by}, \code{levels}, and \code{call}.
}
\description{
Runs \code{\link[=rails]{rails()}} independently within each level of a stratifying variable.
Each subgroup gets its own NPS model, its own selection path, its own stepwise
walk, and its own population margins computed from that subgroup's reference
records, with weights scaled to the subgroup total.
}
\details{
This is the \emph{subgroup calibration} strategy -- "calibration-in-sub" --
that Chen et al. compare against \emph{global calibration}, in which one set of
weights is built from the whole cohort by \code{\link[=rails]{rails()}} and then simply applied
within a subgroup. Which strategy does better is an empirical question rather
than a settled one; that comparison is what the subgroup paper is about, and
heterogeneity of the non-probability selection mechanism across subgroups is
the factor it identifies as most influential. This function provides the
subgroup arm; \code{\link[=rails]{rails()}} provides the global arm.
}
\section{Empty strata}{

Levels with no reference records are skipped with a warning and their
non-probability records receive \code{NA} weights. Factor levels are never
dropped, so model matrices stay aligned between the two samples within each
stratum.
}

\examples{
pop <- rails_simulate(20000, seed = 7)
aou <- pop[pop$aou == 1, ]
ref <- pop[pop$s == 1, ]
ref$dweight <- 1 / ref$ps

fit <- rails_subgroup(aou, ref, vars = c("agegroup", "income"),
                      by = "sex", weights_ref = "dweight",
                      verbose = FALSE)
fit

}
\seealso{
\code{\link[=rails]{rails()}}.
}
