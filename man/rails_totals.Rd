% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rails_cells.R
\name{rails_totals}
\alias{rails_totals}
\title{Population margins matching a cell table}
\usage{
rails_totals(
  cells,
  vars = attr(cells, "vars"),
  order = 3,
  weight_col = "weight"
)
}
\arguments{
\item{cells}{A cell table from \code{\link[=rails_cells]{rails_cells()}}, or any data frame with the
columns in \code{vars} plus a \code{weight} column.}

\item{vars}{Character vector of covariate names. Defaults to the variables
recorded on a \code{rails_cells} object.}

\item{order}{Highest interaction order to compute margins for. Must be at
least as large as the highest order \code{\link[=rails]{rails()}} will select, otherwise raking
has no target for the selected terms.}

\item{weight_col}{Name of the cell-total column.}
}
\value{
A named numeric vector of margins, suitable as the \code{pop_totals}
argument of \code{\link[=rails]{rails()}}.
}
\description{
Builds the named vector of population totals that \code{\link[=rails]{rails()}} rakes to: every
one-way, two-way, ... up to \code{order}-way margin of \code{vars}, named exactly as the
calibration model matrix columns are named. Assembling this by hand is the
most common source of errors, because the names have to match what
\code{\link[survey:calibrate]{survey::calibrate()}} expects.
}
\examples{
pop <- rails_simulate(500, seed = 1)
ref <- rails_cells(pop[pop$s == 1, ], c("agegroup", "sex"))
rails_totals(ref, order = 2)

}
\seealso{
\code{\link[=rails_cells]{rails_cells()}}.
}
