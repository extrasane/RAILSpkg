% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rails_design.R
\name{rails_design}
\alias{rails_design}
\title{Hand RAILS weights to the survey package}
\usage{
rails_design(fit, data, ...)
}
\arguments{
\item{fit}{A \code{rails_fit} from \code{\link[=rails]{rails()}}, or a \code{rails_subgroup_fit} from
\code{\link[=rails_subgroup]{rails_subgroup()}}.}

\item{data}{The data frame the weights belong to: the same non-probability
sample passed to \code{\link[=rails]{rails()}}, with whatever outcome columns you want to
analyse. Must have as many rows as the fit has weights.}

\item{...}{Passed to \code{\link[survey:svydesign]{survey::svydesign()}}.}
}
\value{
A \code{survey.design} object with the RAILS weights attached and a
\code{rails_weights} column added to its data.
}
\description{
Wraps a fit's weights in a \code{\link[survey:svydesign]{survey::svydesign()}} so the usual survey
machinery -- \code{svymean()}, \code{svytotal()}, \code{svyglm()}, \code{svyby()} -- can be used
on the weighted sample.
}
\section{What this design does and does not carry}{

The weights are exact: point estimates from this object -- \code{svymean()},
\code{svytotal()}, \code{svyglm()}, \code{svyby()} -- are the RAILS estimates.

Standard errors are a different matter. The survey package computes its own
design-based variance from the weights it is handed, treating them as fixed.
That is close to, but not the same estimator as, \code{\link[=rails_var]{rails_var()}} with
\code{type = "simplified"}, which is computed independently; in practice the two
agree to about four significant figures, differing by little more than an
n/(n-1) factor. Neither accounts for the weights having been estimated.

So: use this design freely for point estimates and for model fitting, and use
\code{\link[=rails_var]{rails_var()}} when you want inference on a mean that reflects how the weights
were built. Do not expect the two standard errors to match to the last digit --
they are different estimators of the same fixed-weight quantity.
}

\examples{
pop <- rails_simulate(20000, seed = 42)
aou <- pop[pop$aou == 1, ]
ref <- pop[pop$s == 1, ]
ref$dweight <- 1 / ref$ps

fit <- rails(aou, ref, vars = c("agegroup", "sex", "income"),
             weights_ref = "dweight", verbose = FALSE)
des <- rails_design(fit, aou)
survey::svymean(~y, des)

}
\seealso{
\code{\link[=rails_var]{rails_var()}} for corrected inference on a weighted mean.
}
