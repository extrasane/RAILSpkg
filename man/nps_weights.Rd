% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nps_weights.R
\name{nps_weights}
\alias{nps_weights}
\alias{nps}
\title{Nested propensity score weights}
\usage{
nps_weights(
  formula,
  cells_np,
  cells_ref,
  nsiz = sum(cells_ref$weight),
  theta_init = NULL,
  maxit = 1000,
  tol = 1e-10
)
}
\arguments{
\item{formula}{A one-sided model formula, e.g. \code{~ agegroup + sex + agegroup:sex}.}

\item{cells_np}{Aggregated non-probability cell counts, with a column
\code{weight} giving the cell size.}

\item{cells_ref}{Aggregated reference-sample cell counts, with a column
\code{weight}.}

\item{nsiz}{Target population total the weights are scaled to. Defaults to
the total reference-sample weight, \code{sum(cells_ref$weight)}.}

\item{theta_init}{Optional warm-start coefficient vector (e.g. the previous
step's solution, zero-padded for new columns). The pseudo-likelihood is
globally concave, so the warm start only reduces the iteration count.}

\item{maxit}{Maximum Newton-Raphson iterations.}

\item{tol}{Convergence tolerance on the squared Newton step.}
}
\value{
A numeric vector of propensity weights (one per cell), scaled to sum
to \code{nsiz}, with the converged coefficient vector attached as attribute
\code{"theta"}.
}
\description{
Estimates non-probability-sample weights by maximizing the pseudo-likelihood
of the \strong{nested propensity score} (NPS) model via Newton-Raphson on aggregated
covariate cells. The returned weights are scaled to sum to \code{nsiz}.
}
\details{
This is the NPS step of the RAILS algorithm: the pseudo-likelihood merges the
non-probability cohort with the probability sample and the resulting
inverse fitted propensities serve as base weights for the calibration stage.
}
\examples{
pop <- rails_simulate(2000, seed = 1)
np  <- rails_cells(pop[pop$aou == 1, ], c("agegroup", "sex"))
ref <- rails_cells(pop[pop$s == 1, ],   c("agegroup", "sex"))
w   <- nps_weights(~ agegroup + sex, np, ref)
round(w, 2)

}
\seealso{
\code{\link[=rails]{rails()}}, which calls this at the start and at every step of the
stepwise raking walk.
}
