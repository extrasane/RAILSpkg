% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rails_cells.R
\name{rails_cells}
\alias{rails_cells}
\title{Aggregate microdata into covariate cells}
\usage{
rails_cells(data, vars, weights = NULL, drop_na = TRUE)
}
\arguments{
\item{data}{A data frame of individual records.}

\item{vars}{Character vector of covariate names to aggregate over.}

\item{weights}{Optional. Either a numeric vector of length \code{nrow(data)} or
the name of a column of \code{data} holding design weights. \code{NULL} (the default)
counts each row as 1, which is what you want for an unweighted biobank
sample; pass the survey weight for a probability reference sample.}

\item{drop_na}{Drop rows with a missing value in any of \code{vars}. Model
matrices would silently drop them anyway, so the default warns and removes
them up front.}
}
\value{
A data frame with one row per cell: the columns named in \code{vars}, plus
\code{weight}. It carries class \code{rails_cells} and an attribute \code{row_cell}, the
integer index mapping each row of \code{data} to its cell, which is what lets
\code{\link[=rails]{rails()}} return one weight per input record.
}
\description{
Collapses individual-level records into one row per unique combination of
\code{vars}, with a \code{weight} column giving the cell total. RAILS runs entirely on
these cells, so both the non-probability sample and the reference sample must
be aggregated the same way before estimation. \code{\link[=rails]{rails()}} calls this for you
when given microdata; call it directly when you want to inspect or cache the
cell tables.
}
\details{
Factor levels are \strong{not} dropped, so cells present in one sample but empty in
the other still line up in the model matrices.
}
\examples{
pop <- rails_simulate(500, seed = 1)
rails_cells(pop[pop$aou == 1, ], c("agegroup", "sex"))

# A reference sample carrying design weights:
ref <- pop[pop$s == 1, ]
ref$dweight <- 1 / ref$ps
rails_cells(ref, c("agegroup", "sex"), weights = "dweight")

}
\seealso{
\code{\link[=rails_totals]{rails_totals()}} for the matching population margins.
}
