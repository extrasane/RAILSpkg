% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rails_simulate.R
\name{rails_simulate}
\alias{rails_simulate}
\title{Simulate a population with a biased volunteer sample}
\usage{
rails_simulate(
  n,
  alpha = c(-3.5, 0.05, 0.5, 0.2, 0.4, 0.6, 0.01, 0.1, 0.3, 0.1, 0.2, 0.3),
  beta = c(-2.2, 0.03, 0.2, 0.1, 0.2, 0.3, 0.005, 0.05, 0.1, 0.05, 0.1, 0.15),
  gamma = c(-4.8, 0.08, 0.6, 0.3, 0.6, 0.9, 0.02, 0.2, 0.4, 0.15, 0.3, 0.45),
  seed = NULL
)
}
\arguments{
\item{n}{Population size.}

\item{alpha}{Length-12 coefficient vector for the outcome model.}

\item{beta}{Length-12 coefficient vector for probability-sample inclusion.}

\item{gamma}{Length-12 coefficient vector for volunteer-sample inclusion.
Making \code{gamma} differ from \code{beta} is what creates the selection bias RAILS
is meant to remove.}

\item{seed}{Optional integer seed. \code{NULL} leaves the RNG state alone.}
}
\value{
A data frame with one row per population member:
\describe{
\item{\code{id}}{Row identifier.}
\item{\code{agegroup}}{\code{young}, \code{middle}, \code{older}.}
\item{\code{sex}}{\code{male}, \code{female}.}
\item{\code{income}}{\code{p0-10}, \code{p10-50}, \code{p50-90}, \code{p90-100} -- cut at those
percentiles of a truncated-Pareto draw.}
\item{\code{homeown}}{\code{rent}, \code{own}.}
\item{\code{edu}}{\verb{<HS}, \code{HS}, \verb{Some college}, \verb{College+}.}
\item{\code{y},\code{py}}{Binary outcome and its true probability.}
\item{\code{s},\code{ps},\code{fpc}}{Probability-sample indicator, its inclusion
probability, and the population size.}
\item{\code{aou},\code{paou}}{Volunteer-sample indicator and its inclusion
probability.}
}
Design weights for the probability sample are \code{1 / ps}.
}
\description{
Generates the synthetic population used by the RAILS simulation studies: five
categorical covariates, a binary outcome, a probability sample, and a
non-probability ("volunteer") sample whose inclusion depends on the same
covariates. Because the true population is known, the bias of any weighting
method can be read off directly. This is what the examples and vignettes run
on -- real biobank microdata cannot be redistributed.
}
\section{These are synthetic, not calibrated}{

The variable names and level labels are there so worked examples read like the
application rather than like \code{agegroup:sex}. The marginal distributions behind
them are arbitrary and resemble no real population: \code{edu} in particular puts
only 0.5 percent in its top category. Nothing here should be read as a claim
about any real group.
}

\section{Difference from the paper code}{

Income is drawn from a truncated Pareto by inverse-CDF sampling rather than
via \code{distributionsrd::rtruncpareto()}. Same distribution, one less
dependency; streams drawn under a given seed therefore differ from the
simulation archive.
}

\examples{
pop <- rails_simulate(1000, seed = 1)
head(pop[, c("agegroup", "sex", "income", "homeown", "edu", "y")])
table(pop$aou, pop$s)
mean(pop$y)                        # population prevalence
mean(pop$y[pop$aou == 1])          # what the volunteer sample would report

}
