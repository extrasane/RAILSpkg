% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pums.R
\name{pums_recode}
\alias{pums_recode}
\title{Recode raw ACS PUMS into RAILS analysis variables}
\usage{
pums_recode(data, year = 2022, adults_only = TRUE)
}
\arguments{
\item{data}{Raw PUMS records from \code{\link[=pums_fetch]{pums_fetch()}}, or a data frame with the
same ACS column names.}

\item{year}{The ACS vintage \code{data} came from. Only used to check the codings
below are the right ones; see the note.}

\item{adults_only}{Keep only records with \code{AGEP > 17}. \code{TRUE} for a
reference sample of adults. Set \code{FALSE} when you need complete population
counts, for instance to compute state population shares.}
}
\value{
A data frame with one row per retained person and columns \code{weight},
\code{age}, \code{agegroup}, \code{sex}, \code{race_eth}, \code{income}, \code{edu}, \code{homeown}, \code{region},
and, when \code{ST} was fetched, \code{state} as a two-letter USPS code. Demographic
columns may be \code{NA} where a code fell outside the bands.
}
\description{
Applies the banding and labelling used in the RAILS application: age groups,
sex, race and ethnicity, income bands, educational attainment, tenure, and
Census region. Every result is a factor with fixed levels, so cell tables
built from different extracts line up.
}
\section{Codings are vintage-specific}{

The \code{SCHL}, \code{TEN}, \code{SEX}, \code{HISP}, \verb{RAC*} and \code{HINCP} bandings follow the
\strong{2022} ACS 1-year PUMS data dictionary. Category codes do change between
releases, so a different \code{year} raises a warning: check each mapping against
that year's dictionary at
\url{https://www.census.gov/programs-surveys/acs/microdata/documentation.html}
before trusting the output.

Income bands are \verb{<35k}, \verb{35k-50k}, \verb{50k-75k}, \verb{75k-100k}, \verb{>100k}; note that
\code{HINCP} can be negative, and values below -60000 are treated as missing rather
than as the lowest band.
}

\examples{
# A handful of synthetic records in raw ACS coding:
raw <- data.frame(
  PWGTP = c(100, 250, 90), AGEP = c(30, 70, 16), SEX = c(1, 2, 2),
  HINCP = c(42000, 120000, 20000), SCHL = c(21, 16, 12), TEN = c(1, 3, 2),
  HISP = c(1, 5, 1), RACWHT = c(1, 0, 0), RACBLK = c(0, 0, 1),
  RACASN = c(0, 0, 0), REGION = c(1, 4, 3)
)
pums_recode(raw)

}
\seealso{
\code{\link[=pums_fetch]{pums_fetch()}}, \code{\link[=pums_reference]{pums_reference()}}.
}
