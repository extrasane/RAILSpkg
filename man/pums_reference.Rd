% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pums.R
\name{pums_reference}
\alias{pums_reference}
\title{Build a RAILS reference sample from recoded PUMS}
\usage{
pums_reference(
  data,
  vars = c("agegroup", "sex", "edu", "homeown", "income", "race_eth", "region"),
  order = 3,
  rescale = TRUE
)
}
\arguments{
\item{data}{Recoded PUMS from \code{\link[=pums_recode]{pums_recode()}}, or any data frame with a
\code{weight} column and the columns in \code{vars}.}

\item{vars}{Covariates to aggregate over. Defaults to the seven used in the
application.}

\item{order}{Highest interaction order to compute margins for. Must be at
least the \code{scope} you will pass to \code{\link[=rails]{rails()}}.}

\item{rescale}{Rescale the weights of the retained records to the population
total before incomplete records were dropped. Leave \code{TRUE} unless you have a
reason to want the reference sample to under-count.}
}
\value{
An object of class \code{pums_reference}: a list with \code{micro} (the
retained person-level records), \code{cells} (the aggregated table, ready for
\code{rails(..., aggregated = TRUE)}), \code{totals} (the named margin vector for
\code{pop_totals}), \code{n_pop} (the population total the weights sum to), \code{n_dropped}
and \code{vars}.
}
\description{
Takes recoded PUMS microdata and produces the three things RAILS needs from a
reference sample: cleaned person-level records, the aggregated cell table,
and the population margins.
}
\details{
Records with a missing value in any of \code{vars} cannot enter a cell, so they are
dropped -- but dropping them would also lose their share of the population.
The weights are therefore rescaled so the retained records still sum to the
population total computed \emph{before} the drop. This is the step most easily got
wrong by hand, and it is why the totals are worth taking from here rather than
from an ad-hoc aggregation.
}
\examples{
# Standing in for real PUMS, with the same column names and levels:
pop <- rails_simulate(5000, seed = 1)
fake <- data.frame(weight = 1 / pop$ps, pop[, c("agegroup", "sex", "income")])

ref <- pums_reference(fake, vars = c("agegroup", "sex", "income"), order = 2)
ref
head(ref$cells)

}
\seealso{
\code{\link[=pums_fetch]{pums_fetch()}}, \code{\link[=pums_recode]{pums_recode()}}, \code{\link[=rails]{rails()}}.
}
