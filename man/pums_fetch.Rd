% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pums.R
\name{pums_fetch}
\alias{pums_fetch}
\title{Download ACS PUMS microdata from the Census API}
\usage{
pums_fetch(
  year = 2022,
  geography = c("region", "state"),
  variables = NULL,
  key = Sys.getenv("CENSUS_API_KEY"),
  cache = NULL,
  verbose = TRUE
)
}
\arguments{
\item{year}{ACS 1-year vintage, e.g. \code{2022}.}

\item{geography}{\code{"region"} fetches the four Census regions in one request.
\code{"state"} fetches the 50 states plus DC, one request each -- the API caps
how many \code{ucgid} values a single call accepts, so asking for all 51 at once
returns an error page rather than data.}

\item{variables}{Named character vector of ACS variable codes; see
\code{\link[=pums_variables]{pums_variables()}}.}

\item{key}{A Census API key. Free, from
\url{https://api.census.gov/data/key_signup.html}. Defaults to the
\code{CENSUS_API_KEY} environment variable -- set it in \verb{~/.Renviron} rather
than writing the key into a script, since scripts get committed.}

\item{cache}{Optional path to a CSV. If the file exists it is read and no
request is made; otherwise the download is written there. Repeat runs of an
analysis should always set this: the state form is 51 API calls.}

\item{verbose}{Report progress via \code{\link[=message]{message()}}.}
}
\value{
A data frame of raw PUMS records, one row per person, with the
requested variables as numeric columns.
}
\description{
Fetches the person-level PUMS records RAILS uses as its reference sample.
The result is raw ACS codes; pass it to \code{\link[=pums_recode]{pums_recode()}} to get analysis
variables.
}
\section{Network access}{

This function requires an internet connection and the \pkg{jsonlite} package.
Nothing else in RAILS touches the network.
}

\examples{
\dontrun{
# Sys.setenv(CENSUS_API_KEY = "...")   # better: put it in ~/.Renviron
raw <- pums_fetch(2022, cache = "PUMS_2022.csv")
ref <- pums_reference(pums_recode(raw))
}

}
\seealso{
\code{\link[=pums_recode]{pums_recode()}} to turn the result into analysis variables, then
\code{\link[=pums_reference]{pums_reference()}} to get cells and margins.
}
