% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{weights.rails_fit}
\alias{weights.rails_fit}
\alias{weights.rails_subgroup_fit}
\title{Extract RAILS weights}
\usage{
\method{weights}{rails_fit}(object, ...)

\method{weights}{rails_subgroup_fit}(object, ...)
}
\arguments{
\item{object}{A \code{rails_fit} or \code{rails_subgroup_fit}.}

\item{...}{Ignored.}
}
\value{
A numeric vector: one weight per record of the non-probability sample
that was fitted, or one per cell when the fit was built from cell tables.
Records dropped for missing covariates, and strata with no reference
records, are \code{NA}.
}
\description{
Extract RAILS weights
}
\examples{
pop <- rails_simulate(2000, seed = 3)
ref <- pop[pop$s == 1, ]; ref$dweight <- 1 / ref$ps
fit <- rails(pop[pop$aou == 1, ], ref, c("agegroup", "sex"),
             weights_ref = "dweight", verbose = FALSE)
summary(weights(fit))

}
