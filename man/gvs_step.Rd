% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gvs_step.R
\name{gvs_step}
\alias{gvs_step}
\alias{gvs}
\title{Greedy variable selection: test one candidate interaction}
\usage{
gvs_step(
  x,
  terms_current,
  loglik,
  cells_ref,
  cells_np,
  m_ref,
  maxit = 1000,
  tol = 0.001
)
}
\arguments{
\item{x}{Term or terms to add, e.g. \code{"agegroup:sex:income"}. A vector is
tested as a single increment, which is how \code{\link[=rails]{rails()}} keeps a selected
higher-order term together with any lower-order components it needs.}

\item{terms_current}{Character vector of terms already in the current model.}

\item{loglik}{Log-likelihood of the current model.}

\item{cells_ref}{Aggregated reference-sample cell counts (column \code{weight}).}

\item{cells_np}{Aggregated non-probability cell counts (column \code{weight}).}

\item{m_ref}{Model matrix for the current model on \code{cells_ref}, used only for
its column count, to compute degrees of freedom.}

\item{maxit}{Maximum Newton-Raphson iterations.}

\item{tol}{Convergence tolerance on the squared Newton step. Looser than
\code{\link[=nps_weights]{nps_weights()}} because only the log-likelihood is needed, not the weights.}
}
\value{
A length-5 numeric vector \code{c(loglik, LRT, p-value, LRT/df, df)}, or
\code{rep(NA, 5)} if any cell count is zero or the solver fails.
}
\description{
Fits the nested propensity score model with one candidate interaction term
added and returns a likelihood-ratio test of that term against the current
model. \code{\link[=rails]{rails()}} calls this once per candidate at every round of greedy
variable selection (GVS), the forward step of the RAILS algorithm; the
candidate with the largest significant increment per degree of freedom is the
one added.
}
\seealso{
\code{\link[=rails]{rails()}}, which runs the GVS loop, and \code{\link[=nps_weights]{nps_weights()}} for the
model being tested.
}
